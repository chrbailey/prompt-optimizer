{
  "name": "@prompt-optimizer/action",
  "version": "1.0.0",
  "description": "GitHub Action for deterministic prompt quality scoring",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "test": "jest",
    "lint": "eslint src/**/*.ts"
  },
  "keywords": [
    "github-action",
    "prompt-engineering",
    "llm",
    "quality-gate",
    "ci-cd",
    "prompt-scoring"
  ],
  "author": "chrbailey",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/glob": "^0.4.0",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.3.0"
  }
}
